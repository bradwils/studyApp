# TEST ONLY: verify @claude mention support

Throwaway file for end-to-end verification that `@claude` in a PR
comment actually triggers a reply after the permission fix. Will be
deleted without merging once confirmed.
