// TEST ONLY: deliberate bug to verify the plain-prompt claude-review
// posts a real comment. Will be deleted without merging once confirmed.
func maxOfTwo(_ a: Int, _ b: Int) -> Int {
    return a < b ? a : b
}
